import 'package:flutter/material.dart';

class SupplyWidget extends StatelessWidget {
  /// TODO add your comment here
  const SupplyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
